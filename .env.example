SUPABASE_URL=https://btgszhfcvmovqtvantce.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Secret de signature des sessions JWT (32+ caracteres). Generer avec: openssl rand -base64 32
SESSION_SECRET=change-me-to-a-random-32-plus-character-secret

# Stripe (mode test recommande pour commencer: cles pk_test_/sk_test_)
STRIPE_SECRET_KEY=sk_test_your_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
NEXT_PUBLIC_APP_URL=http://localhost:3000

# A renseigner dans billing_plans.stripe_price_id (table Supabase) une fois les
# produits/prix crees dans le Dashboard Stripe (mode recurring, mensuel):
#   starter      -> price_xxx
#   professional -> price_yyy
# (enterprise reste sans stripe_price_id : bouton "Nous contacter")
